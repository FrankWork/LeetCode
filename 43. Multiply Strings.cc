#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    string multiply(string num1, string num2) {
        
    }
};

int main(){
  Solution so;
  string result;

  result = so.multiply("0", "0");
  cout << result << endl << endl;
}