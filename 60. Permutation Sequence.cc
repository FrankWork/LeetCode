#include <string>
#include <iostream>

using namespace std;

class Solution {
public:
    string getPermutation(int n, int k) {
        
    }
};

int main(){
  Solution so;

  cout << so.getPermutation(0, 0) << endl;
  cout << so.getPermutation(0, 0) << endl;
  cout << so.getPermutation(0, 0) << endl;
}
