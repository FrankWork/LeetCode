#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

class Solution {
public:
    vector<int> GetLeastNumbers_Solution(vector<int> input, int k) {
        
    }
};

int main(){
  Solution so;
  vector<int> nums{4,5,1,6,2,7,3,};
  vector<int> res = so.GetLeastNumbers_Solution(nums);
  for(auto n:res)
  cout<<n<<endl;
}